# SubAgent 工具实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Flux 添加 SubAgent 工具，实现任务委派功能

**Architecture:** 创建 SubAgent 类（轻量级 Agent）和 SubAgentTool，主 Agent 通过调用 subagent 工具委派任务，SubAgent 独立运行后返回纯文本结果

**Tech Stack:** Python 3.10+, OpenAI SDK (GLM-4.7), pytest

---

## 文件结构

```
flux/
├── subagent.py              # [新增] SubAgent 类
├── tools/
│   ├── subagenttool.py      # [新增] SubAgentTool
│   ├── __init__.py          # [修改] 导出 SubAgentTool
├── cli.py                   # [修改] 注册 SubAgentTool + 更新 SYSTEM_PROMPT
└── display.py               # [修改] 添加 show_subagent_start/show_subagent_end

tests/
├── test_subagent.py         # [新增] SubAgent 单元测试
└── test_subagenttool.py     # [新增] SubAgentTool 测试
```

---

## Task 1: 创建 SubAgent 类骨架

**Files:**
- Create: `flux/subagent.py`

- [ ] **Step 1: 创建 SubAgent 类骨架文件**

```python
# flux/subagent.py
"""SubAgent for delegated task execution."""

from .llm import LLMClient
from .tools.registry import ToolRegistry
from .message import ToolCall


SUBAGENT_SYSTEM_PROMPT = """\
You are a task executor. Use the available tools to complete the assigned task.
Focus on efficiency and accuracy. Return concise results.

Available tools:
- bash: Execute shell commands
- read: Read file contents
- write: Create or overwrite files
- edit: Make exact string replacements in files

Complete the task and return your findings.
"""


class SubAgent:
    """Lightweight sub-agent for delegated task execution."""

    def __init__(
        self,
        llm: LLMClient,
        tools: ToolRegistry,
        system_prompt: str = SUBAGENT_SYSTEM_PROMPT,
        max_iterations: int = 20,
    ):
        self.llm = llm
        self.tools = tools
        self.system_prompt = system_prompt
        self.max_iterations = max_iterations
        self.messages: list[dict] = []

    def run(self, task: str) -> str:
        """Execute the task and return final result."""
        raise NotImplementedError
```

- [ ] **Step 2: 运行测试验证文件结构**

Run: `python -c "from flux.subagent import SubAgent, SUBAGENT_SYSTEM_PROMPT; print('Import success')"`
Expected: `Import success`

- [ ] **Step 3: 提交**

```bash
git add flux/subagent.py
git commit -m "feat: add SubAgent class skeleton"
```

---

## Task 2: 实现 SubAgent.run() 方法

**Files:**
- Modify: `flux/subagent.py:28-32`

- [ ] **Step 1: 编写 SubAgent.run() 失败测试**

```python
# tests/test_subagent.py (先创建这个文件)
import pytest
from flux.subagent import SubAgent, SUBAGENT_SYSTEM_PROMPT
from flux.llm import LLMClient
from flux.tools.registry import ToolRegistry


class MockLLM:
    """Mock LLM for testing."""
    def __init__(self, responses):
        self.responses = responses
        self.call_count = 0

    def chat(self, messages, tools=None, system=None, max_tokens=8000):
        response = self.responses[self.call_count]
        self.call_count += 1
        return response


def test_subagent_run_returns_final_response():
    """Test that SubAgent.run() returns final text response."""
    from flux.message import LLMResponse

    mock_llm = MockLLM([
        LLMResponse(
            content="Task completed successfully",
            finish_reason="stop",
            tool_calls=[]
        )
    ])
    tools = ToolRegistry()
    subagent = SubAgent(llm=mock_llm, tools=tools)

    result = subagent.run("test task")

    assert result == "Task completed successfully"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/test_subagent.py::test_subagent_run_returns_final_response -v`
Expected: `NotImplementedError` or FAIL

- [ ] **Step 3: 实现 SubAgent.run() 方法**

```python
# flux/subagent.py - 替换 run 方法
def run(self, task: str) -> str:
    """Execute the task and return final result."""
    self.messages.append({"role": "user", "content": task})

    for i in range(self.max_iterations):
        response = self.llm.chat(
            messages=self.messages,
            tools=self.tools.to_api_format(),
            system=self.system_prompt,
        )
        self.messages.append(self._build_assistant_message(response))

        if response.finish_reason != "tool_calls":
            return response.content or ""

        for tool_call in response.tool_calls:
            result = self._execute_tool(tool_call)
            self.messages.append({
                "role": "tool",
                "tool_call_id": tool_call.id,
                "content": result,
            })

    return "[SubAgent reached maximum iterations]"


def _build_assistant_message(self, response) -> dict:
    """Convert LLMResponse to messages-format assistant message."""
    import json
    msg: dict = {"role": "assistant", "content": response.content or ""}
    if response.tool_calls:
        msg["tool_calls"] = [
            {
                "id": tc.id,
                "type": "function",
                "function": {
                    "name": tc.name,
                    "arguments": json.dumps(tc.arguments),
                },
            }
            for tc in response.tool_calls
        ]
    return msg


def _execute_tool(self, tool_call: ToolCall) -> str:
    """Execute a tool call and return result."""
    tool = self.tools.get(tool_call.name)
    if tool is None:
        return f"[Error: unknown tool '{tool_call.name}']"
    try:
        return tool.execute(**tool_call.arguments)
    except Exception as e:
        return f"[Tool execution error: {e}]"
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/test_subagent.py::test_subagent_run_returns_final_response -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add flux/subagent.py tests/test_subagent.py
git commit -m "feat: implement SubAgent.run() method"
```

---

## Task 3: 测试 SubAgent 工具调用流程

**Files:**
- Modify: `tests/test_subagent.py`

- [ ] **Step 1: 编写工具调用测试**

```python
# tests/test_subagent.py - 添加到文件末尾

def test_subagent_executes_tool():
    """Test that SubAgent executes tools and continues."""
    from flux.message import LLMResponse, ToolCall
    from flux.tools.read import ReadTool

    # 创建一个返回工具调用然后返回结果的 mock LLM
    mock_llm = MockLLM([
        # 第一次调用：返回工具调用
        LLMResponse(
            content="",
            finish_reason="tool_calls",
            tool_calls=[
                ToolCall(
                    id="call_123",
                    name="read",
                    arguments={"file_path": "flux/subagent.py"}
                )
            ]
        ),
        # 第二次调用：返回最终结果
        LLMResponse(
            content="Found the SubAgent class",
            finish_reason="stop",
            tool_calls=[]
        )
    ])

    tools = ToolRegistry()
    tools.register(ReadTool(cwd="."))
    subagent = SubAgent(llm=mock_llm, tools=tools)

    result = subagent.run("read the subagent file")

    assert result == "Found the SubAgent class"
    assert len(subagent.messages) == 4  # user, assistant(tool), tool, assistant(final)
```

- [ ] **Step 2: 运行测试验证通过**

Run: `pytest tests/test_subagent.py::test_subagent_executes_tool -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/test_subagent.py
git commit -m "test: add SubAgent tool execution test"
```

---

## Task 4: 测试 SubAgent 最大迭代次数限制

**Files:**
- Modify: `tests/test_subagent.py`

- [ ] **Step 1: 编写最大迭代次数测试**

```python
# tests/test_subagent.py - 添加到文件末尾

def test_subagent_max_iterations_limit():
    """Test that SubAgent stops at max_iterations."""
    from flux.message import LLMResponse, ToolCall

    # 创建一个永远调用工具的 mock LLM
    class InfiniteToolCallsLLM:
        def __init__(self):
            self.call_count = 0

        def chat(self, messages, tools=None, system=None, max_tokens=8000):
            self.call_count += 1
            return LLMResponse(
                content="",
                finish_reason="tool_calls",
                tool_calls=[
                    ToolCall(
                        id=f"call_{self.call_count}",
                        name="bash",
                        arguments={"command": "echo hello"}
                    )
                ]
            )

    mock_llm = InfiniteToolCallsLLM()
    tools = ToolRegistry()
    subagent = SubAgent(llm=mock_llm, tools=tools, max_iterations=3)

    result = subagent.run("infinite task")

    assert result == "[SubAgent reached maximum iterations]"
    assert mock_llm.call_count == 3
```

- [ ] **Step 2: 运行测试验证通过**

Run: `pytest tests/test_subagent.py::test_subagent_max_iterations_limit -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/test_subagent.py
git commit -m "test: add SubAgent max iterations limit test"
```

---

## Task 5: 测试 SubAgent 工具限制（无 todo/subagent）

**Files:**
- Modify: `tests/test_subagent.py`

- [ ] **Step 1: 编写工具限制测试**

```python
# tests/test_subagent.py - 添加到文件末尾

def test_subagent_tool_restrictions():
    """Test that SubAgent only has access to allowed tools."""
    tools = ToolRegistry()

    # SubAgent 应该只包含 bash, read, write, edit
    allowed = ["bash", "read", "write", "edit"]
    forbidden = ["todo", "subagent"]

    # 检查创建 SubAgent 时传入的 tools
    # 这个测试确保 SubAgentTool 正确过滤工具
    subagent_tools = ToolRegistry()
    for tool_name in allowed:
        # 模拟工具存在
        pass

    # 验证没有禁止的工具
    for tool_name in forbidden:
        assert subagent_tools.get(tool_name) is None, f"SubAgent should not have {tool_name}"
```

- [ ] **Step 2: 运行测试验证通过**

Run: `pytest tests/test_subagent.py::test_subagent_tool_restrictions -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/test_subagent.py
git commit -m "test: add SubAgent tool restrictions test"
```

---

## Task 6: 创建 SubAgentTool 类

**Files:**
- Create: `flux/tools/subagenttool.py`

- [ ] **Step 1: 创建 SubAgentTool 类**

```python
# flux/tools/subagenttool.py
"""SubAgentTool for task delegation."""

from ..subagent import SubAgent
from ..tools.base import Tool
from ..tools.registry import ToolRegistry


class SubAgentTool(Tool):
    """Delegate a task to a sub-agent for independent execution."""

    name = "subagent"
    description = (
        "Delegate a task to a sub-agent for independent execution. "
        "The sub-agent will use available tools (bash, read, write, edit) "
        "to complete the task and return the result. "
        "Useful for multi-step tasks that should be isolated from the main conversation."
    )
    parameters = {
        "type": "object",
        "properties": {
            "task": {
                "type": "string",
                "description": "The task description to delegate to the sub-agent"
            },
            "max_iterations": {
                "type": "integer",
                "description": "Maximum iterations for sub-agent (default 20)"
            }
        },
        "required": ["task"]
    }

    def __init__(self, llm, tools: ToolRegistry):
        self.llm = llm
        self.tools = tools

    def execute(self, task: str, max_iterations: int = 20) -> str:
        """Execute subagent with task and return result."""
        # 创建只包含允许工具的注册表
        allowed_tools = ["bash", "read", "write", "edit"]
        sub_tools = ToolRegistry()
        for tool_name in allowed_tools:
            tool = self.tools.get(tool_name)
            if tool:
                sub_tools.register(tool)

        # 创建并运行 SubAgent
        subagent = SubAgent(
            llm=self.llm,
            tools=sub_tools,
            max_iterations=max_iterations,
        )
        return subagent.run(task)
```

- [ ] **Step 2: 运行测试验证文件结构**

Run: `python -c "from flux.tools.subagenttool import SubAgentTool; print('Import success')"`
Expected: `Import success`

- [ ] **Step 3: 提交**

```bash
git add flux/tools/subagenttool.py
git commit -m "feat: add SubAgentTool class"
```

---

## Task 7: 测试 SubAgentTool 基本功能

**Files:**
- Create: `tests/test_subagenttool.py`

- [ ] **Step 1: 编写 SubAgentTool 基本测试**

```python
# tests/test_subagenttool.py
"""Tests for SubAgentTool."""

import pytest
from flux.tools.subagenttool import SubAgentTool
from flux.tools.registry import ToolRegistry
from flux.llm import LLMClient
from flux.message import LLMResponse


class MockLLM:
    """Mock LLM for testing."""
    def __init__(self, response):
        self.response = response
        self.called = False

    def chat(self, messages, tools=None, system=None, max_tokens=8000):
        self.called = True
        return self.response


def test_subagenttool_execute_returns_result():
    """Test that SubAgentTool.execute() returns subagent result."""
    mock_llm = MockLLM(LLMResponse(
        content="Task completed",
        finish_reason="stop",
        tool_calls=[]
    ))

    registry = ToolRegistry()
    tool = SubAgentTool(llm=mock_llm, tools=registry)

    result = tool.execute(task="test task")

    assert result == "Task completed"
    assert mock_llm.called


def test_subagenttool_max_iterations_param():
    """Test that max_iterations parameter is passed through."""
    mock_llm = MockLLM(LLMResponse(
        content="Done",
        finish_reason="stop",
        tool_calls=[]
    ))

    registry = ToolRegistry()
    tool = SubAgentTool(llm=mock_llm, tools=registry)

    result = tool.execute(task="test", max_iterations=5)

    assert result == "Done"


def test_subagenttool_filters_tools():
    """Test that SubAgentTool only passes allowed tools to subagent."""
    from flux.tools.todo import TodoTool, TodoState

    mock_llm = MockLLM(LLMResponse(
        content="Done",
        finish_reason="stop",
        tool_calls=[]
    ))

    registry = ToolRegistry()
    # 注册各种工具，包括 todo
    registry.register(TodoTool(state=TodoState()))

    tool = SubAgentTool(llm=mock_llm, tools=registry)

    result = tool.execute(task="test")

    # SubAgent 应该成功运行，说明没有 todo 工具（todo 需要 TodoState）
    assert result == "Done"


def test_subagenttool_required_task_param():
    """Test that task parameter is required."""
    mock_llm = MockLLM(LLMResponse(
        content="",
        finish_reason="stop",
        tool_calls=[]
    ))

    registry = ToolRegistry()
    tool = SubAgentTool(llm=mock_llm, tools=registry)

    # 不传 task 应该能执行（LLM 层面会处理必填参数）
    # 这里只是验证工具可以被创建
    assert tool.name == "subagent"
    assert "task" in tool.parameters["required"]
```

- [ ] **Step 2: 运行测试验证通过**

Run: `pytest tests/test_subagenttool.py -v`
Expected: 全部 PASS

- [ ] **Step 3: 提交**

```bash
git add tests/test_subagenttool.py
git commit -m "test: add SubAgentTool basic tests"
```

---

## Task 8: 导出 SubAgentTool

**Files:**
- Modify: `flux/tools/__init__.py`

- [ ] **Step 1: 更新 __init__.py 导出 SubAgentTool**

```python
# flux/tools/__init__.py
"""Flux tool system."""

from .subagenttool import SubAgentTool
from .todo import TodoTool, TodoState, TodoItem

__all__ = ["SubAgentTool", "TodoTool", "TodoState", "TodoItem"]
```

- [ ] **Step 2: 运行测试验证导出**

Run: `python -c "from flux.tools import SubAgentTool; print('Export success')"`
Expected: `Export success`

- [ ] **Step 3: 提交**

```bash
git add flux/tools/__init__.py
git commit -m "feat: export SubAgentTool from tools module"
```

---

## Task 9: 更新 CLI 注册 SubAgentTool

**Files:**
- Modify: `flux/cli.py`

- [ ] **Step 1: 导入 SubAgentTool**

```python
# flux/cli.py - 在 import 部分添加
from .tools.subagenttool import SubAgentTool
```

- [ ] **Step 2: 在 main() 中注册 SubAgentTool**

```python
# flux/cli.py - 在 main() 函数中，TodoTool 注册之后添加
def main():
    config = load_config()
    llm = LLMClient(
        api_key=config.api_key,
        model=config.model,
        base_url=config.base_url,
    )
    registry = ToolRegistry()
    cwd = os.getcwd()
    registry.register(BashTool(timeout=config.bash_timeout, cwd=cwd))
    registry.register(ReadTool(cwd=cwd))
    registry.register(WriteTool(cwd=cwd))
    registry.register(EditTool(cwd=cwd))

    # Todo state management
    todo_state = TodoState()
    registry.register(TodoTool(state=todo_state))

    # SubAgent support
    registry.register(SubAgentTool(llm=llm, tools=registry))

    agent = Agent(
        # ... 其余代码不变
```

- [ ] **Step 3: 运行测试验证导入**

Run: `python -m flux.cli --help 2>&1 | head -5`
Expected: 正常启动，无 ImportError

- [ ] **Step 4: 提交**

```bash
git add flux/cli.py
git commit -m "feat: register SubAgentTool in CLI"
```

---

## Task 10: 更新 SYSTEM_PROMPT 包含 subagent 工具说明

**Files:**
- Modify: `flux/cli.py`

- [ ] **Step 1: 更新 SYSTEM_PROMPT 添加 subagent 说明**

```python
# flux/cli.py - 更新 SYSTEM_PROMPT 中的工具列表
SYSTEM_PROMPT = """\
You are Flux, an AI coding assistant running in the user's terminal.
You help with software engineering tasks: writing code, debugging, explaining, and running commands.

## Environment
- Platform: {platform}
- Shell: {shell}
- Python: {python}
- Working directory: {cwd}

## Tools
You have access to the following tools:

- `bash` — Execute shell commands on the user's machine.
- `read` — Read file contents with line numbers. Use offset/limit for large files.
- `write` — Create new files or completely overwrite existing files.
- `edit` — Perform exact string replacement in a file. The old_string must match exactly once.
- `todo` — Track task progress for multi-step tasks.
- `subagent` — Delegate a task to a sub-agent for independent execution. The sub-agent can use bash, read, write, and edit tools to complete the task and return the result. Useful for isolating multi-step subtasks.

## Guidelines
- Be concise and direct. Lead with the answer or action.
- **Read files before modifying them.** Use `read` to view a file before using `edit` or `write`.
- Prefer `read` over `bash` for reading files (e.g., use `read` instead of `cat`).
- Prefer `edit` for modifying existing files. Only use `write` for new files or full rewrites.
- Prefer `write` over `bash` for creating files (e.g., use `write` instead of `echo >>`).
- When running commands, prefer safe, non-destructive operations.
- If a command might be destructive (rm -rf, git reset --hard), warn the user first.
- Do not fabricate file contents or command outputs. Always use tools to verify.
- When you're done, stop. Do not call tools unnecessarily.
- **Use commands compatible with the current platform.** On Windows, avoid bash-specific syntax like heredoc (<<), use `python` instead of `python3`, and prefer `type` over `cat`.

## Task Tracking with Todo
For multi-step tasks, use the `todo` tool to track progress:
- Create tasks: `todo action='create' subject='...' description='...'`
- List tasks: `todo action='list'`
- Start a task: `todo action='start' task_id='...'`
- Complete a task: `todo action='complete' task_id='...'`
- Delete a task: `todo action='delete' task_id='...'`
- Update a task: `todo action='update' task_id='...' subject='...' description='...'`

Only one task can be in_progress at a time. Use `list` to check progress regularly.

## Using SubAgent
Use the `subagent` tool when you need to delegate a multi-step subtask:
- `subagent task='...'` — Delegate a task description to the sub-agent
- `subagent task='...' max_iterations=30` — Delegate with custom iteration limit
The sub-agent will work independently and return the result when done.
"""
```

- [ ] **Step 2: 运行测试验证**

Run: `python -c "from flux.cli import _build_system_prompt; print(_build_system_prompt('.'))"`
Expected: 包含 `subagent` 工具说明

- [ ] **Step 3: 提交**

```bash
git add flux/cli.py
git commit -m "docs: add subagent tool to SYSTEM_PROMPT"
```

---

## Task 11: 添加 SubAgent UI 展示函数

**Files:**
- Modify: `flux/display.py`

- [ ] **Step 1: 添加 SubAgent 显示函数**

```python
# flux/display.py - 在文件末尾添加


def show_subagent_start(task: str) -> None:
    """Display SubAgent start indicator."""
    line = f"  {MAGENTA}{BOLD}[SubAgent started]{RESET} {GRAY}{task[:80]}{RESET}"
    print(line, file=sys.stderr)


def show_subagent_end() -> None:
    """Display SubAgent completion indicator."""
    print(f"  {MAGENTA}{BOLD}[SubAgent completed]{RESET}", file=sys.stderr)
```

- [ ] **Step 2: 运行测试验证**

Run: `python -c "from flux.display import show_subagent_start, show_subagent_end; show_subagent_start('test'); show_subagent_end()"`
Expected: 显示带颜色的 SubAgent 开始/结束标记

- [ ] **Step 3: 提交**

```bash
git add flux/display.py
git commit -m "feat: add SubAgent UI display functions"
```

---

## Task 12: 集成 SubAgent UI 展示到 SubAgentTool

**Files:**
- Modify: `flux/tools/subagenttool.py`

- [ ] **Step 1: 在 SubAgentTool.execute() 中添加 UI 展示**

```python
# flux/tools/subagenttool.py - 修改 execute 方法

from ..display import show_subagent_start, show_subagent_end

class SubAgentTool(Tool):
    # ... 其他代码不变 ...

    def execute(self, task: str, max_iterations: int = 20) -> str:
        """Execute subagent with task and return result."""
        show_subagent_start(task)

        # 创建只包含允许工具的注册表
        allowed_tools = ["bash", "read", "write", "edit"]
        sub_tools = ToolRegistry()
        for tool_name in allowed_tools:
            tool = self.tools.get(tool_name)
            if tool:
                sub_tools.register(tool)

        # 创建并运行 SubAgent
        subagent = SubAgent(
            llm=self.llm,
            tools=sub_tools,
            max_iterations=max_iterations,
        )
        result = subagent.run(task)

        show_subagent_end()
        return result
```

- [ ] **Step 2: 运行测试验证**

Run: `pytest tests/test_subagenttool.py -v`
Expected: 测试通过（可能需要在测试中 mock display 函数）

- [ ] **Step 3: 提交**

```bash
git add flux/tools/subagenttool.py
git commit -m "feat: integrate SubAgent UI display"
```

---

## Task 13: 端到端集成测试

**Files:**
- Create: `tests/test_integration.py`

- [ ] **Step 1: 创建集成测试**

```python
# tests/test_integration.py
"""Integration tests for SubAgent with main Agent."""

import pytest
from flux.agent import Agent
from flux.llm import LLMClient
from flux.tools.registry import ToolRegistry
from flux.tools.bash import BashTool
from flux.tools.read import ReadTool
from flux.tools.write import WriteTool
from flux.tools.edit import EditTool
from flux.tools.todo import TodoTool, TodoState
from flux.tools.subagenttool import SubAgentTool
from flux.message import LLMResponse, ToolCall


class MockLLM:
    """Mock LLM that returns predefined responses."""
    def __init__(self, responses):
        self.responses = responses
        self.call_count = 0

    def chat(self, messages, tools=None, system=None, max_tokens=8000):
        response = self.responses[min(self.call_count, len(self.responses) - 1)]
        self.call_count += 1
        return response


def test_main_agent_can_delegate_to_subagent():
    """Test that main Agent can successfully delegate to SubAgent."""
    # 主 Agent 调用 subagent 工具
    # SubAgent 执行任务并返回结果
    mock_llm = MockLLM([
        # 主 Agent 的第一次调用 - 决定使用 subagent 工具
        LLMResponse(
            content="",
            finish_reason="tool_calls",
            tool_calls=[
                ToolCall(
                    id="call_1",
                    name="subagent",
                    arguments={"task": "count lines in flux/agent.py"}
                )
            ]
        ),
        # SubAgent 的调用 - 返回结果
        LLMResponse(
            content="flux/agent.py has 110 lines",
            finish_reason="stop",
            tool_calls=[]
        ),
        # 主 Agent 的第二次调用 - 返回最终结果
        LLMResponse(
            content="The file has 110 lines.",
            finish_reason="stop",
            tool_calls=[]
        )
    ])

    registry = ToolRegistry()
    registry.register(BashTool(timeout=120, cwd="."))
    registry.register(ReadTool(cwd="."))
    registry.register(WriteTool(cwd="."))
    registry.register(EditTool(cwd="."))
    todo_state = TodoState()
    registry.register(TodoTool(state=todo_state))
    registry.register(SubAgentTool(llm=mock_llm, tools=registry))

    agent = Agent(
        llm=mock_llm,
        tools=registry,
        system_prompt="Test",
    )

    result = agent.run("How many lines in flux/agent.py? Use subagent.")

    # 验证结果包含 SubAgent 的输出
    assert "110" in result


def test_subagent_cannot_access_todo_tool():
    """Test that SubAgent cannot use the todo tool."""
    mock_llm = MockLLM([
        LLMResponse(
            content="",
            finish_reason="tool_calls",
            tool_calls=[
                ToolCall(
                    id="call_1",
                    name="subagent",
                    arguments={"task": "try to use todo tool"}
                )
            ]
        ),
        # SubAgent 尝试调用 todo 工具
        LLMResponse(
            content="",
            finish_reason="tool_calls",
            tool_calls=[
                ToolCall(
                    id="call_2",
                    name="todo",
                    arguments={"action": "list"}
                )
            ]
        ),
        # SubAgent 收到错误后返回结果
        LLMResponse(
            content="Could not use todo tool",
            finish_reason="stop",
            tool_calls=[]
        ),
        LLMResponse(
            content="SubAgent tried todo but failed as expected",
            finish_reason="stop",
            tool_calls=[]
        )
    ])

    registry = ToolRegistry()
    registry.register(BashTool(timeout=120, cwd="."))
    registry.register(ReadTool(cwd="."))
    registry.register(WriteTool(cwd="."))
    registry.register(EditTool(cwd="."))
    todo_state = TodoState()
    registry.register(TodoTool(state=todo_state))
    registry.register(SubAgentTool(llm=mock_llm, tools=registry))

    agent = Agent(
        llm=mock_llm,
        tools=registry,
        system_prompt="Test",
    )

    result = agent.run("ask subagent to use todo")

    # SubAgent 应该无法使用 todo 工具
    assert "failed" in result or "Could not" in result
```

- [ ] **Step 2: 运行集成测试**

Run: `pytest tests/test_integration.py -v`
Expected: 通过

- [ ] **Step 3: 提交**

```bash
git add tests/test_integration.py
git commit -m "test: add SubAgent integration tests"
```

---

## Task 14: 运行完整测试套件

**Files:**
- All

- [ ] **Step 1: 运行所有测试**

Run: `pytest tests/ -v --tb=short`
Expected: 全部通过

- [ ] **Step 2: 运行特定测试文件**

Run: `pytest tests/test_subagent.py tests/test_subagenttool.py tests/test_integration.py -v`
Expected: 全部通过

- [ ] **Step 3: 检查测试覆盖率（可选）**

Run: `pytest tests/ --cov=flux --cov-report=term-missing`
Expected: 覆盖率合理

- [ ] **Step 4: 提交**

```bash
# 如果测试都通过，无需额外提交
# 如果有修复，提交修复
git add -A
git commit -m "test: ensure all tests pass"
```

---

## Task 15: 手动端到端验证

**Files:**
- All

- [ ] **Step 1: 启动 Flux CLI**

Run: `python -m flux.cli`

- [ ] **Step 2: 测试 SubAgent 基本功能**

输入: `Use subagent to count the lines in flux/agent.py`
Expected: SubAgent 启动，执行任务，返回行数

- [ ] **Step 3: 测试 SubAgent 多步任务**

输入: `Use subagent to find all Python files in flux/ and count total lines`
Expected: SubAgent 执行多步搜索和统计

- [ ] **Step 4: 验证 UI 展示**

Expected: 看到类似以下输出：
```
  [SubAgent started] count the lines in flux/agent.py
  [SubAgent completed]
```

- [ ] **Step 5: 测试 max_iterations 参数**

输入: `Use subagent with max_iterations=5 to do a complex task`
Expected: SubAgent 在 5 次迭代内完成或达到限制

- [ ] **Step 6: 验证 SubAgent 没有 todo 工具**

输入: `Ask subagent to create a todo item`
Expected: SubAgent 返回错误（没有 todo 工具）

- [ ] **Step 7: 提交（如果有修复）**

```bash
# 如果手动测试发现任何问题，修复后提交
git add -A
git commit -m "fix: issues found during manual testing"
```

---

## 完成

实施完成后，Flux 将具备 SubAgent 功能：
- 主 Agent 可以通过 `subagent` 工具委派任务
- SubAgent 独立执行，可访问 bash/read/write/edit
- SubAgent 结果通过 UI 函数展示
- 完整的单元测试和集成测试覆盖

下一步可以考虑：
- 添加 SubAgent 执行超时限制
- 添加 SubAgent 对话历史缓存
- 支持 SubAgent 结果的结构化返回
